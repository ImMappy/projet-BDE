{% extends './base.html.twig' %}

{% block title %}Lieu index{% endblock %}

{% block body %}
    <div class="d-flex justify-content-start mx-4 my-4  ">
        <a class="btn btn-primary"href="{{ path('sortie_index') }}">Retour a l'accueil</a>

    </div>
    <h1 class="text-center my-4">Lieu index</h1>
    <table class="table table-striped table-hover w-75 mx-auto border rounded ">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Rue</th>
                <th>Latitude</th>
                <th>Longitude</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lieu in lieus %}
            <tr>
                <td>{{ lieu.nom }}</td>
                <td>{{ lieu.rue }}</td>
                <td>{{ lieu.latitude }}</td>
                <td>{{ lieu.longitude }}</td>
                <td>
                    <a class="btn btn-primary"href="{{ path('lieu_show', {'id': lieu.id}) }}">Détail</a>
                    {% if is_granted("ROLE_ADMIN") %}
                    <a class="btn btn-warning" href="{{ path('lieu_edit', {'id': lieu.id}) }}">Modifier</a>
                    {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="d-flex justify-content-center">
        <a class="btn btn-success my-4 text-center"href="{{ path('lieu_new') }}">Ajouter un lieu</a>

    </div>
{% endblock %}
