{% extends './base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="d-flex justify-content-start mx-4 my-4  ">
        <a class="btn btn-primary"href="{{ path('lieu_index') }}">Retour a la liste</a>
    </div>

    <h1 class="text-center my-3">Modifier profil</h1>
    <section class="w-75 mx-auto">
        {{ form_start(form) }}
            <div class="form-group my-2">
                {{ form_row(form.email) }}
            </div>
            <div class="form-group my-2">
                {{ form_row(form.nom) }}
            </div>
            <div class="form-group my-2">
                {{ form_row(form.prenom) }}
            </div>
             <div class="form-check my-2">
                {{ form_row(form.password) }}
            </div>
            <div class="form-group my-2">
                {{ form_row(form.telephone) }}
            </div>
            <div class="form-group my-2">
                {{ form_row(form.Roles) }}
            </div>  <div class="form-group my-2">
                {{ form_row(form.image) }}
            </div>
        {% if  is_granted("ROLE_ADMIN") %}
            <div class="form-check my-2">
            {{ form_row(form.isAdministrateur) }}
            </div>
            <div class="form-check my-2">
            {{ form_row(form.isActif) }}
            </div>
        {% endif %}
    </section>
    <div class="d-flex justify-content-center">
        {{ form_widget(form.submit) }}
    </div>
            {{ form_end(form) }}




{#    {{ include('pages/user/_form.html.twig') }}#}

    {% if  is_granted("ROLE_ADMIN") %}
    {{ include('pages/user/_delete_form.html.twig') }}
    {% endif %}

{% endblock %}
